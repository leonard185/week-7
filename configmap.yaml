apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: shop
  labels:
    app.kubernetes.io/name: web
    app.kubernetes.io/part-of: cart-app
data:
  APP_PORT: "8080"
  # Connection string format your app expects (example for Postgres):
  DATABASE_URL: "postgres://shop:shop123@db.shop.svc.cluster.local:5432/shopdb"
